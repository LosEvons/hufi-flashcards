Structure of DB objects:
Card with two sides and an assigned group
A group

Structure of pages:
Main page -> list of groups (creation of group) -> list of cards (creation of cards) -> view&edit singular card